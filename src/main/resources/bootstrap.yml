spring:
  cloud:
    kubernetes:
      discovery:
        enabled: false
      config:
        sources:
#          - namespace: ${CONFIG_NAMESPACE}
#            name: ${CONFIG_NAME}}
          - namespace: blog
          - name: comments-service-config
        enabled: true